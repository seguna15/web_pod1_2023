/**
 * condition ? executes if true : executes if false
 */

const accountStatus = confirm('Are you sure you want to deactivate your account');

accountStatus ? alert('Account deactivated') : alert('User is still active');

alert('programme has finished running');